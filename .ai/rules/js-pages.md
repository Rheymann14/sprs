---
paths:
  - resources/js/pages/statistics.tsx
---

# Js Pages

## Statistics groups use count cards
Render each Year + Incident Type + Subcategory statistics entry as its own heading followed by a responsive card row. The row contains Total Incident Count plus one count card per configured managed status; do not present these grouped entries as a data table.
